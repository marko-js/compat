$ {
  input.renderBody(out);
  input.renderThing(out);
  input.renderBody(out, attrs);
  renderBody(out);
}

$ input.template.render({ x: 1 }, out);
$ input.template.renderer({ y() {} }, out);
$ input.barRenderer(null, out);
$ state.barRenderer(null, out);
$ data.barRenderer(null, out);
$ x.barRenderer(null, out);

$ input.barRenderer({}, true, out);

$ if (x) {
  renderA(out);
} else if (y) {
  renderB(out);
} else {
  renderC(out);
}

$ x ? renderA(out) : y ? renderB(out) : renderC(out);

$ x && render(out);
$ x || render(out);

$ for (let i = 0; i < 10; i++) {
  input.items[i].renderBody(out);
}

$ let j = 10;
$ while (j--) {
  input.renderBody(out);
}
