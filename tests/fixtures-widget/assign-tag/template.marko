<var firstName="John" lastName="Smith" fullName/>
<assign fullName="${firstName} ${lastName}"/>

<div>${fullName}</div>

<var count=0/>
<assign count++/>

<assign count++ if(count === 1)/>

<div>${count}</div>
